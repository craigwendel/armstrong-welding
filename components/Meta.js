import Head from 'next/head';

const Meta = () => (
  <Head>
    <meta charSet="utf-8" />
    <meta
      name="viewport"
      content="minimum-scale=1, initial-scale=1, width=device-width"
    />
    <title>Armstrong Welding</title>
  </Head>
);

export default Meta;
